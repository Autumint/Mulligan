SMODS.Consumable {
    no_collection = true,
    key = "curdletext",
    set = "taintedcards",
    eternal_compat = true,

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "lemegetontext",
    set = "taintedcards",
    eternal_compat = true,

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "greedtext",
    set = "taintedcards",
    eternal_compat = true,

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "wealthtext",
    set = "taintedcards",
    eternal_compat = true,

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "phantasmtext",
    set = "taintedcards",
    eternal_compat = true,

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "wastelandtext",
    set = "taintedcards",
    eternal_compat = true,

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "shatteredtext",
    set = "taintedcards",
    eternal_compat = true,

    in_pool = function(self)
        return false
    end,
}

