SMODS.Consumable {
    no_collection = true,
    key = "curdletext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "lemegetontext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "greedtext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "wealthtext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "phantasmtext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "wastelandtext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "shatteredtext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "skiptext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "errtext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "fliptext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

SMODS.Consumable {
    no_collection = true,
    key = "erodetext",
    set = "taintedcards",

    in_pool = function(self)
        return false
    end,
}

